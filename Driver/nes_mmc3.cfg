MEMORY {
    ZP:         start = $0000, size = $0100, type = rw, define = yes;
    HEADER:     start = $0000, size = $0010, file = "", fill = yes;
    ROM0:       start = $8000, size = $2000, file = "dsp_mmc3_code.bin", fill = yes;
    ROMS:       start = $fea4, size = $0156, file = "dsp_mmc3_data.bin", fill = yes;
    ROMV:       start = $fffa, size = $0006, file = "dsp_mmc3_data.bin", fill = yes;
    ROMC:       start = $0000, size = $2000, file = "dsp_mmc3_data.bin", fill = yes;
    STACK:      start = $0100, size = $0200, type = rw, define = yes;
    RAM:        start = $0200, size = $0600, type = rw, define = yes;
}

SEGMENTS {
    HEADER:     load = HEADER, type = ro;
    CODE:       load = ROM0,   type = ro, define = yes;
    MUSDATA:    load = ROM0,   type = ro, define = yes;
    PCMDATA:    load = ROM0,   type = ro, optional = yes;
    STARTUP:    load = ROMS,   type = ro, define = yes;
    LOWCODE:    load = ROMS,   type = ro, optional = yes;
    INIT:       load = ROMS,   type = ro, define = yes, optional = yes;
    RODATA:     load = ROMS,   type = ro, define = yes;
    VECTORS:    load = ROMV,   type = ro;
    CHARS:      load = ROMC,   type = ro;
    BSS:        load = RAM,    type = bss, define = yes;
    ZEROPAGE:   load = ZP,     type = zp;
}
